{
  "version": "0.2.0",
  "configurations": [
      {
        "type": "node",
        "request": "attach",
        "name": "Launch Program",
        "program": "${workspaceFolder}/ts/index.ts"

      }
    ]
}